spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/compass
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
 
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
 

 
server:
  port: 8080
